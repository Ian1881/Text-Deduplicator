# Text-Deduplicator
A client-side web tool I built to help my coworkers process text data from messy, duplicated sheet imports. It normalizes string inputs by stripping punctuation and matching case to split unique content from duplicate rows instantly.
